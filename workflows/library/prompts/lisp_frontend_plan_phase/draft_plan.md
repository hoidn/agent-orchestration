Draft an execution-ready plan for the selected full-design work item.

Use the consumed full design, MVP design, work-item context, and progress
ledger. Treat the full design as the active implementation target and the MVP
design as historical context and compatibility guidance for completed MVP scope.
The plan must be self-contained enough that implementation can execute from the
approved plan without rediscovering scope.

Write the plan to the relpath recorded in `plan_path.txt`.
