Draft an execution-ready plan for the selected Lisp frontend work item.

Use the consumed full design, MVP design, work-item context, and progress
ledger. The plan must be self-contained enough that implementation can execute
from the approved plan without rediscovering scope.

Write the plan to the relpath recorded in `plan_path.txt`.
