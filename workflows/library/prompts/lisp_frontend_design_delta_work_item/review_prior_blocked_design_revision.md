You are reviewing whether the revised target design resolves a prior
implementation roadmap conflict without weakening the baseline design
compatibility contract.

Read the consumed target design, baseline design, blocker progress report, and
revision report. Decide `APPROVE` if the target design now provides a coherent
contract for replanning. Decide `REVISE` if a bounded follow-up edit to the
target design should fix a concrete issue.

The injected `blocked-design-revision-review-target-path.txt` file contains the
exact workspace-relative path for the review report. Write the review report at
that path and the review decision at the required decision path.
