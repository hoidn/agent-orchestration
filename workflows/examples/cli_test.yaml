version: "1.1"
name: cli_test
processed_dir: processed
steps:
  - name: create_file
    command: ["touch", "processed/test.txt"]

  - name: list_processed
    command: ["ls", "-la", "processed/"]
    output_capture: lines