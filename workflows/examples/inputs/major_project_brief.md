# Major Project Demo Brief

Port a small analysis subsystem to a new backend while preserving the existing user-facing behavior.

The project is intentionally broad enough to require decomposition. It needs documentation discovery, an architecture baseline, a first backend interface, and later numerical feature slices.

This fixture is used only to validate the major-project tranche workflow shape.
