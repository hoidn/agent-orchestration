# Probe-Mischaracterization Stress Test Seed

Reviewer 3 asks for a numerical stress test that injects varying degrees of error into the assumed probe. The revision study should start from this seed, let the design passes refine it into the approved working design, then plan and implement the study with explicit provenance and manuscript claim gates.

Scope constraints:
- Do not add trainable-probe or joint-probe-recovery variants unless a later approved design changes scope.
- Keep true measurement data fixed when testing wrong assumed probes.
- Record probe-source, perturbation, baseline-comparability, and paper-export provenance before adding manuscript claims.
