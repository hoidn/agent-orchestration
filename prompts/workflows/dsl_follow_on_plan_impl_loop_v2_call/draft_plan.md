Read the `Consumed Artifacts` section first and treat it as the authoritative input list.
Read the consumed `design` artifact before acting.

Draft a concrete implementation plan for landing the design in this repo.
Write the plan to the workspace-relative path stored in `state/follow-on-plan-phase/plan_path.txt`.

Keep it scoped, actionable, and specific about files, risks, and verification.
Do not widen scope beyond the design.
