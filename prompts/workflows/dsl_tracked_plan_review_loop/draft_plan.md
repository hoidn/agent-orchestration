Read the `Consumed Artifacts` section first and treat it as the authoritative input list.
Read the consumed `design` artifact before acting.

Draft an implementation plan derived from the design.
Update the current plan in place at the workspace-relative path stored in `state/plan_path.txt`.

Keep the plan actionable, staged, and verification-oriented.
