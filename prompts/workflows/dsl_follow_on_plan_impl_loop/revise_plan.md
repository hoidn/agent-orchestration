use receiving-code-review to address the feedback

Read the `Consumed Artifacts` section first and treat it as the authoritative input list.
Read the consumed `design`, `plan`, and `plan_review_report` artifacts before acting.

Update the current plan in place at the consumed `plan` artifact path so it addresses the review and stays aligned with the design.
Leave the plan ready for another review pass.
