# Lisp Frontend Steering

Own the Workflow Lisp frontend MVP and full design implementation.

The drain may choose either:

- an active backlog item that already describes the next Lisp frontend task; or
- an unimplemented part of the Lisp frontend MVP/full design, then draft a
  bounded implementation architecture for that gap.

Keep selector judgment local and keep workflow routing deterministic.
