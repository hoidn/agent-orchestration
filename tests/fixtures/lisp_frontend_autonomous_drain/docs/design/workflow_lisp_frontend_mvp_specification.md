# Workflow Lisp Frontend MVP Specification

## Stage 1: Frontend Core Without Workflow Execution

Implement parser, syntax objects, modules, imports, exports, type declarations,
and basic type checking.

## Stage 2: Procedural Substrate

Implement `defworkflow`, `defproc`, `call`, `let*`, `match`, effect signatures,
source maps, and Core AST lowering.
