# Source Guidance

This task points at the program-level `nanobragg_run` call chain derived from `nanoBragg.c`'s `main` simulation path.

Focus on the post-parse simulation region that computes the detector float image.
Do not treat this as a CLI-clone task.
