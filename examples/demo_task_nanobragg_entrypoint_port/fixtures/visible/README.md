# nanoBragg Entrypoint Visible Fixtures

These fixtures describe raw input cases for the `nanobragg_run` reference entrypoint.

Each visible case provides:

- `case_id`
- `argv`
- `output_shape`

The fixture values are authoritative inputs for the task.
Hidden expected outputs are not included here.
